services:
  postgres:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: teamrouter
      POSTGRES_PASSWORD: teamrouter
      POSTGRES_DB: teamrouter
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  app:
    build: .
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      DATABASE_URL: "postgresql://teamrouter:teamrouter@postgres:5432/teamrouter?schema=public"
      ENCRYPTION_KEY: "${ENCRYPTION_KEY}"
      ANTHROPIC_API_KEY: "${ANTHROPIC_API_KEY}"
      APP_URL: "${APP_URL:-http://localhost:3000}"
    depends_on:
      - postgres

volumes:
  pgdata:
